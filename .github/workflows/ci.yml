name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        run: npx vite build

      # The economy harnesses: save round-trip and migrations, the hauling
      # ledger, the soft-lock guard, oxen accounting.
      - name: Harnesses
        run: npm test

      # The determinism rope. A red run here means the simulation changed
      # behaviour — sometimes on purpose, in which case the goldens are
      # re-recorded in the same commit that changed it.
      - name: Determinism
        run: npm run rope

      # Until Pages is switched on, this is how a build gets played: every
      # green run leaves a downloadable, self-contained copy of the game.
      - name: Playable build
        uses: actions/upload-artifact@v4
        with:
          name: cozy-village-${{ github.sha }}
          path: dist
          retention-days: 30
