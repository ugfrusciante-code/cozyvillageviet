# Dependencies
node_modules/

# Build output
dist/

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local-only config and scratch
*.local
.claude/settings.local.json

# Baked art sources. The OBJs are tens of megabytes each and what ships is the
# baked output in src/render/*-data.ts; the .mtl files stay tracked so the
# material definitions live in history.
assets/*.obj
